events {}
http {
    upstream app {
        server 127.0.0.1:8081; # default blue
    }

    server {
        listen 80;

        location / {
            proxy_pass http://app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
